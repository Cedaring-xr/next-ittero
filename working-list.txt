Working list of additions

Testing
    -- debug why webkit tests are failing
        -- may be do to outdated version of macOS because of older laptop
            -- Try to run tests on desktop and see if same errors occur

    -- setup test account with maps email (done)
    -- create playwright tests for sign-up (failing)
    -- create playwright tests for sign-out (failing)
    -- switch tests to using variable names for URL instead of hard coding them


    future : create tests around sign-up process that deletes user after completion



API integration
    -- create lambda api connection based on ride-share demo
    -- create API gateway endpoints for basic list crud opperations

    -- create dynamoDB table for list items


